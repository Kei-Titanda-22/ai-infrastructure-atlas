{
  "name": "ai-infrastructure-atlas",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "engines": {"node": ">=22.19.0"},
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && pagefind --site dist",
    "preview": "astro preview",
    "validate:data": "python scripts/validate-data.py && python scripts/validate-facilities.py && python scripts/validate-audits.py && python scripts/validate-v02.py && python scripts/validate-v03.py && python scripts/validate-v04.py",
    "validate:secrets": "python scripts/check-secrets.py",
    "validate": "npm run validate:data && npm run validate:secrets"
  },
  "dependencies": {"astro": "7.2.9"},
  "devDependencies": {"pagefind": "1.5.2"}
}
